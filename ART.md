# 美术资源需求清单

给 game-alpha（俯视角生存肉鸽）换皮用。当前游戏内 **全部对象都是代码画的纯色圆形/矩形占位**，本清单按代码里真实存在的绘制对象逐条列出，可以直接发给美术，也可以自己照着做。

> 结论：**共 90 张图**（必做 88 张 + 2 张可选），分 P0 实体 / P1 UI / P2 特效 / 平台传播 四类。格式以 **PNG-24（带透明通道）** 为主，只有大面积无透明背景用 JPG。**不要 GIF**。

---

## 一、规格约定（先看这节，能避免返工）

| 项 | 约定 | 原因 |
|---|---|---|
| 设计基准 | 画布逻辑宽 375 × 高 667（`js/consts.js` 的 canvasW/canvasH） | UI 全部按屏幕宽度自适应 |
| 导出倍率 | 统一 **@2x** 一套（逻辑 32px → 导出 64px）；主角 / Boss / 首页背景可额外给 @3x | 手机 DPR 2~3，一套 @2x 体积与清晰度最平衡 |
| 主体格式 | **PNG-24（RGBA）** | 需要透明通道 |
| 背景格式 | **JPG，质量 80**，无透明 | 大面积照片/氛围图，PNG 体积过大 |
| 可选优化 | 上线前统一转 **WebP**（小游戏支持），体积再降 30~50% | 包体超限时的第一手段 |
| 锚点 | **中心锚点，四周必须对称留白** | 游戏按实体中心坐标定位，贴图偏心会直接表现为"图没对准碰撞圈" |
| 图集留白 | 合并图集时每格四周留 **2px 全透明** | 防止采样串色（texture bleeding） |
| 图集尺寸 | 单张 ≤ **1024×1024**，2 的幂 | 老机型纹理上限保守值 |
| 朝向 | 主角与**子弹一律头朝右（0°）**；怪物可选「头朝右」或「正面朝上」两种画法 | 主角、子弹始终按 `Math.atan2(vy, vx)` 旋到朝向/飞行方向；怪物是否旋转由 `js/npc/monster/config.js` 的 `SPRITE_ROTATES` 决定 |
| 动画 | 序列帧横向/网格图集，不用 GIF；帧率 8~12fps | GIF 无半透明且解码慢 |
| 命名 | 全小写 + 下划线 + ASCII，例 `mob_tank_02.png` | 禁止中文/空格/大写，避免真机路径大小写踩坑 |
| 目录 | 建议 `images/entity/`、`images/ui/`、`images/fx/`、`images/atlas/` | 与 `js/` 模块划分对齐 |
| 包体 | 小游戏主包 4MB / 整包上限以官方最新文档为准；本清单 90 张 @2x 估算 < 1MB | 超限时优先转 WebP 或走在线资源 |

**风格建议**：深色底（当前背景 `#0d0d1a` / 地面 `#1a1a2e`）+ 高饱和主体色，保留现有配色作为**识别色**（玩家靠颜色分辨怪类型；病原体本身没有颜色可言，所以颜色完全按游戏需要定）：

| 对象 | 现有识别色（建议保留） |
|---|---|
| 主角（防疫人员） | `#3498db` 蓝 |
| 刺头（冠状病毒 · 普通怪） | `#16a085` 青 |
| 窜子（甲型流感 · 快速怪） | `#e67e22` 橙 |
| 铁坨（金葡菌 · 坦克怪） | `#8e44ad` 紫 |
| 喷子（丝状病毒 · 远程怪） | `#e74c3c` 红 |
| 金匣（质粒 · 宝箱怪） / 宝箱 | `#f39c12` 金 |
| 毒王（人巨细胞病毒 · Boss1） | `#c0392b` 暗红 |
| 跟班 / 跟班子弹 | `#5dade2` 浅蓝 |
| 主角子弹 | `#fff` 白 |
| 经验宝石 | `#2ecc71` 绿 |
| 暴击飘字 / 标题强调 | `#f1c40f` 黄 |

---

## 二、P0 游戏实体（必做，36 张）

对应"进游戏就能看见的东西"。逻辑尺寸按代码里的真实碰撞半径换算，美术按 **@2x** 列导出。

### 2.1 角色（4）

| 文件 | 内容 | 逻辑尺寸 | @2x 导出 | 格式 | 数量 |
|---|---|---|---|---|---|
| `player_idle.png` | 主角待机（俯视持枪，`PLAYER_SPRITE_SIZE=42`）：蓝雨衣 + 圆帽 + 背包的防疫人员，双手据枪；**按「枪口朝右（+x）」出图**，运行时旋到索敌方向，枪口永远指着目标 | 42×42 | 84×84 | PNG | 1 |
| `player_walk_01.png` `player_walk_02.png` | 走路两帧（上下浮动/腿部），同样枪口朝右 | 42×42 | 84×84 | PNG | 2 |
| `player_shield.png` | 护盾环（角色半径 +5） | 48×48 | 96×96 | PNG | 1 |

> 受击闪烁、护盾呼吸脉冲、枪口火光都是代码 `globalAlpha` / 程序绘制实现，**不需要单独出图**。
> 显示尺寸 `PLAYER_SPRITE_SIZE` 与碰撞半径 `PLAYER_RADIUS` 分开配：横向持枪剪影按宽等比缩放后身体会偏小，靠前者调大小，后者不动手感。

### 2.2 跟班（1）

| 文件 | 内容 | 逻辑尺寸 | @2x | 格式 | 数量 |
|---|---|---|---|---|---|
| `companion.png` | 跟班本体（`COMPANION_RADIUS=8`，环绕主角，数量不定） | 16×16 | 32×32 | PNG | 1 |

### 2.3 子弹（4）

| 文件 | 内容 | 逻辑尺寸 | @2x | 格式 | 数量 |
|---|---|---|---|---|---|
| `bullet_player.png` | 主角子弹（半径 5，朝右画，可拖尾） | 12×12 | 24×24 | PNG | 1 |
| `bullet_companion.png` | 跟班子弹（半径 3，浅蓝，比主角小一号） | 8×8 | 16×16 | PNG | 1 |
| `bullet_enemy.png` | 远程怪子弹（半径 4，红） | 8×8 | 16×16 | PNG | 1 |
| `bullet_boss.png` | Boss 环形弹幕弹体（半径 6，暗红，需比小怪子弹更醒目） | 12×12 | 24×24 | PNG | 1 |

> `bullet_companion` 若能靠代码缩放 `bullet_player` 复用，可省 1 张。

### 2.4 普通怪（15，每只 idle + 2 走路帧）

| 文件前缀 | 内容 | 逻辑尺寸 | @2x | 帧数 |
|---|---|---|---|---|
| `mob_basic_` | **刺头**（近战追击杂兵）：球形衣壳 + 一圈放射状棒状刺突（"冠状"轮廓），正中一颗亮核 | 28×28 | 56×56 | 3 |
| `mob_fast_` | **窜子**（血薄速快）：更小的椭球，刺突长短不齐像被随机改过，壳上一道分节裂缝 | 20×20 | 40×40 | 3 |
| `mob_tank_` | **铁坨**（血厚体大）：好几颗球菌粘成一串葡萄状团块，外裹一层厚荚膜，**没有刺突**（靠轮廓和刺头区分） | 44×44 | 88×88 | 3 |
| `mob_ranged_` | **喷子**（站桩喷射）：一根打结盘绕的丝状杆，像缠起来的线团，一端开口外翻成喷口 | 26×26 | 52×52 | 3 |
| `mob_chest_` | **金匣**（击杀掉宝箱）：环状 DNA 盘成的金色囊泡，表面伸出一根钩状性菌毛（闪烁由代码控制） | 32×32 | 64×64 | 3 |

> 走路帧嫌多可以只出 idle（每类 1 张，共 5 张），代码做上下浮动 + 朝向翻转，先跑起来再补帧。
> 出图统一**俯视 + 上下镜像对称**；走「头朝右（+x）」画法时运行时按「朝向玩家」旋转（`SPRITE_ROTATES = true`），走正面朝上画法则保持正立，见第九节。
> **这六张还欠着**：主题已从昆虫换成微生物，`images/entity/` 里跑的仍是第一批 Q 版造型，六张都要按上表重出，Boss 最优先。

### 2.5 Boss（4）

| 文件 | 内容 | 逻辑尺寸 | @2x | 格式 | 数量 |
|---|---|---|---|---|---|
| `boss1_idle_01.png` `boss1_idle_02.png` | 毒王待机两帧（体型最大，`radius=34`）：一圈变形虫状伪足的多核巨细胞，体内 4~6 颗发亮的核，外膜上稀稀拉拉挂着从宿主偷来的包膜刺突 | 68×68 | 136×136 | PNG | 2 |
| `boss1_charge.png` | 冲锋姿态（对应真实「细胞融合扩散」，`chargeSpeed=520`）：伪足全部朝前拉成一片，身体压扁成楔形 | 68×68 | 136×136 | PNG | 1 |
| `boss1_cast.png` | 施法姿态（环形弹幕=病毒血症爆发 / 召唤=潜伏再激活 共用）：外膜刺突全部张开，核群聚到中心发亮 | 68×68 | 136×136 | PNG | 1 |

> 冲锋外圈红色警示圈由代码画（`boss.js`），也可换成 `fx_boss_ring.png`（见 P2）。后续新 Boss 按此 4 张一组追加。

### 2.6 掉落物（3）

| 文件 | 内容 | 逻辑尺寸 | @2x | 格式 | 数量 |
|---|---|---|---|---|---|
| `xpgem.png` | 经验宝石 = **抗原碎片**（病原体裂解后掉的小碎片，菱形/水滴，半径 6，会被磁吸） | 12×12 | 24×24 | PNG | 1 |
| `chest.png` | 宝箱（可拾取物，半径 12，上下浮动） | 24×24 | 48×48 | PNG | 1 |
| `chest_glow.png` | 宝箱外圈光晕 | 36×36 | 72×72 | PNG | 1 |

### 2.7 地面与边界（4）

| 文件 | 内容 | 逻辑尺寸 | @2x | 格式 | 数量 |
|---|---|---|---|---|---|
| `tile_ground.png` | 地面，**必须可无缝平铺**（`TILE=64`，地图 2000×2000） | 64×64 | 128×128 | PNG/JPG | 1 |
| `tile_decor_01.png` `tile_decor_02.png` | 地面装饰变体（草丛/石块/裂缝，随机摆放打破重复感） | 64×64 | 128×128 | PNG | 2 |
| `border_wall.png` | 地图边界条（横向；纵向由代码旋转 90°，省一张） | 128×16 | 256×32 | PNG | 1 |

### 2.8 通用（1）

| 文件 | 内容 | 逻辑尺寸 | @2x | 格式 | 数量 |
|---|---|---|---|---|---|
| `shadow_ellipse.png` | 通用椭圆软阴影，所有角色/怪共用，靠代码缩放 | 128×32 | 256×64 | PNG | 1 |

**P0 小计：36 张**

---

## 三、P1 界面 UI（36 张，其中 2 张可选）

### 3.1 HUD（12）

| 文件 | 内容 | 逻辑尺寸 | @2x | 格式 | 数量 |
|---|---|---|---|---|---|
| `hud_hp_frame.png` | 血条底框（当前 180×18） | 180×18 | 360×36 | PNG | 1 |
| `hud_hp_fill.png` | 血条填充（**可横向拉伸**的 9-patch 小图即可） | 32×18 | 64×36 | PNG | 1 |
| `hud_xp_frame.png` | 经验条底框（180×10） | 180×10 | 360×20 | PNG | 1 |
| `hud_xp_fill.png` | 经验条填充 | 32×10 | 64×20 | PNG | 1 |
| `hud_boss_frame.png` | 顶部 Boss 血条底（≤300×10，居中） | 300×10 | 600×20 | PNG | 1 |
| `hud_boss_fill.png` | 顶部 Boss 血条填充 | 32×10 | 64×20 | PNG | 1 |
| `icon_heart.png` | 生命图标 | 24×24 | 48×48 | PNG | 1 |
| `icon_gem.png` | 经验图标 | 24×24 | 48×48 | PNG | 1 |
| `icon_clock.png` | 生存计时图标 | 24×24 | 48×48 | PNG | 1 |
| `icon_skull.png` | 击杀图标 | 24×24 | 48×48 | PNG | 1 |
| `icon_chest.png` | 开箱图标 | 24×24 | 48×48 | PNG | 1 |
| `ui_panel_bg.png` | 通用半透明面板底（**9-patch**，HUD/弹窗/结算共用） | 128×128 | 256×256 | PNG | 1 |

### 3.2 升级三选一（14）

卡片实际尺寸由屏幕宽度算出（375 宽下约 **110×143**），按 128×168 出图再缩放。

| 文件 | 内容 | @2x | 数量 |
|---|---|---|---|
| `upgrade_card.png` | 卡片底 | 256×336 | 1 |
| `upgrade_card_sel.png` | 选中态（当前是黄描边 + 放大动画） | 256×336 | 1 |
| `upgrade_rarity_common.png` `..._rare.png` `..._epic.png` | 稀有度边框（对应 TODO「宝箱种类设计」的分级） | 256×336 | 3 |
| `stat_icon_maxHp.png` `stat_icon_speed.png` `stat_icon_attack.png` `stat_icon_defence.png` `stat_icon_atkSpeed.png` `stat_icon_attackRange.png` `stat_icon_critRate.png` `stat_icon_luck.png` | **8 个升级项图标，文件名必须与 `UPGRADES` 的 key 一一对应**（代码按 key 拼图标路径） | 64×64 | 8 |
| `title_levelup.png` | 「LEVEL UP!」艺术字（**可选**，暂用系统字体） | 320×96 | 1 |

### 3.3 首页（10）

| 文件 | 内容 | 逻辑尺寸 | @2x | 格式 | 数量 |
|---|---|---|---|---|---|
| `home_bg.jpg` | 首页全屏背景（竖屏） | 375×667 | 750×1334 | JPG | 1 |
| `title_logo.png` | 游戏标题字（**可选**：名字仍是暂定 `game-alpha`，定稿前建议先用文字绘制，别浪费一张） | 300×80 | 600×160 | PNG | 1 |
| `ui_btn_primary.png` | 主按钮底（9-patch，绿色开始游戏） | 96×48 | 192×96 | PNG | 1 |
| `ui_btn_ghost.png` | 次按钮底（9-patch，设置/图鉴/记录） | 96×48 | 192×96 | PNG | 1 |
| `icon_settings.png` `icon_codex.png` `icon_records.png` `icon_back.png` | 四个入口图标（禁用态由代码降 alpha） | 40×40 | 80×80 | PNG | 4 |
| `ui_switch_track.png` `ui_switch_thumb.png` | 设置页开关底 + 圆点 | 88×44 / 44×44 | 176×88 / 88×88 | PNG | 2 |

> 首页怪物图鉴的圆形头像**直接复用 `mob_*.png` / `boss1_idle_01.png`**，不需要额外出图。

**P1 小计：36 张**

---

## 四、P2 特效（16 张，可后置）

| 文件 | 内容 | 逻辑尺寸 | @2x | 格式 | 数量 |
|---|---|---|---|---|---|
| `fx_hit_01..04.png` | 命中火花 4 帧 | 24×24 | 48×48 | PNG | 4 |
| `fx_death_01..04.png` | 怪物死亡消散 4 帧 | 32×32 | 64×64 | PNG | 4 |
| `fx_levelup_01..06.png` | 升级爆发光环 6 帧 | 64×64 | 128×128 | PNG | 6 |
| `fx_boss_ring.png` | Boss 冲锋警示圈（替换 `boss.js` 里代码画的红色圆环） | 96×96 | 192×192 | PNG | 1 |
| `fx_chest_light.png` | 开箱光柱 | 64×64 | 128×128 | PNG | 1 |

**P2 小计：16 张**

---

## 五、平台/传播（2 张，非游戏内）

| 文件 | 内容 | 尺寸 | 格式 | 数量 |
|---|---|---|---|---|
| `app_avatar.png` | 小程序后台头像（后台要求：正方形、不小于 144×144、不超 2M；建议按 1024×1024 出源文件再缩放） | 1024×1024 | PNG/JPG | 1 |
| `share_card.png` | 分享卡片图（竖屏游戏建议 5:4，文字避开四角） | 500×400 | JPG/PNG | 1 |

---

## 六、明确不需要出图的部分（省工）

- **伤害飘字**：系统字体 + 代码上浮淡出，已实现（`js/fx/damageText.js`）。除非要做像素字体风格，否则不用出图。
- **中文字体**：不要嵌字体文件（中文字库 5~15MB，包体直接爆）。全部用系统字体。
- **怪物头顶血条 / 摇杆圆盘 / 地面网格线 / 升级卡片文字 / 结算文字**：均为程序绘制，保留即可。
- **图鉴解锁态灰框、开关"未接入"置灰**：代码用 alpha + 色块实现。

---

## 七、现有旧素材处理建议（`images/` 25 个文件，都是飞机大战模板遗留）

| 文件 | 尺寸 | 判断 |
|---|---|---|
| `hero.png` | 186×130 | 废弃 —— 飞机造型，与俯视圆角色不匹配 |
| `enemy.png` | 120×79 | 废弃 |
| `bullet.png` | 62×108 | 废弃 —— 竖向长条，本作子弹需朝右画再旋转 |
| `bg.jpg` | 512×512 | 留作 `home_bg` 氛围参考，不直接复用（方图，竖屏 750×1334 需重做） |
| `Common.png` | 512×512 | 模板图集，需人工确认有无可用 UI 件，预期价值很低 |
| `explosion1..19.png` | 64×48 ×19 | **唯一有复用价值的一组** —— 可直接当 P2 的 `fx_death` / Boss 死亡特效基础，风格统一后再决定是否重绘 |

`audio/`（bgm.mp3、boom.mp3、bullet.mp3）同为遗留素材，属 TODO「音效设计」范围，不在本清单内。

---

## 八、数量汇总

| 分类 | 张数 | 必做 | 说明 |
|---|---|---|---|
| P0 游戏实体 | 36 | 36 | 换皮最小集，做完游戏就"不像占位图"了 |
| P1 界面 UI | 36 | 34 | 2 张可选（升级艺术字、标题 logo） |
| P2 特效 | 16 | 16 | 可后置，先用旧 explosion 序列顶上 |
| 平台传播 | 2 | 2 | 头像 + 分享卡 |
| **合计** | **90** | **88** | 其中含动画帧；按逻辑素材约 60 项 |

交付时建议合并成 **4~5 张图集**（`atlas_entity.png` 1024×1024、`atlas_ui.png` 1024×1024、`atlas_fx.png` 512×512 等）+ 一份帧/位置 JSON，减少真机贴图加载次数。

---

## 九、贴图接入状态（已实现，2026-09-13）

`js/base/sprite.js` 提供 `drawSprite(ctx, angle = 0, alpha = 1)`：

- **中心锚点**：按 `x/y` 为中心绘制，`width/height` 为显示尺寸，四周留白对称即可对齐碰撞圈
- **朝向旋转**：`angle` 传 `Math.atan2(vy, vx)`，只旋转「头朝右（+x）」出图的批次；怪物是否旋转由 `js/npc/monster/config.js` 的 `SPRITE_ROTATES` 控制
- **未加载完自动回退**：贴图没到位时返回 `false`，调用方继续画原来的纯色圆/方块。换皮是逐张生效，不需要一次性改完
- **按 src 缓存**：`new Enemy` 每刷一只都会构造一次，缓存避免重复创建与解码图片

已接线：`Player`（枪口朝右出图，转到 `Math.atan2(dirY, dirX)`；受击闪烁走 `alpha`）、`Enemy`（宝箱怪整体闪烁）、`Boss`（`this.angle` 始终在算，是否用由开关决定）。
**未接线**：跟班、双方子弹、经验宝石、宝箱、地面与阴影 —— 仍是代码绘制，等各自贴图到位再逐个替换。

贴图路径来源：`js/consts.js` 的 `PLAYER_SPRITE`、`js/npc/monster/config.js` 每个怪类型的 `sprite`。改碰撞半径后重跑一次抠图脚本即可重出 @2x 尺寸。

### 当前批次（2026-09-13 回退）

`images/entity/` 里六只怪仍是**第一批 Q 版贴图**：正面朝上绘制，所以 `SPRITE_ROTATES = false`。
「头朝右」的昆虫批次原图留在 `vibe_images/insect_v1/`，要切回去：把该目录里的文件移回 `vibe_images/`，重跑 10.2 的抠图脚本，再把 `SPRITE_ROTATES` 改成 `true`。

`player_idle.png` 已换成**俯视持枪批次**（枪口朝右；角色不受 `SPRITE_ROTATES` 影响，始终旋转），子弹与枪口火光都从身体前方 `PLAYER_MUZZLE_LEN` 处出发，开火看起来是从枪管里打出去的。

---

## 十、AI 出图 + 抠图流水线（自用，无需美术）

### 10.1 prompt 模板

出图模型对英文更稳，中文只用于图鉴文案。每条 prompt 按这个骨架填：

```text
Top-down 2D game sprite of a single <病原体英文名，带一句形态描述>,
seen directly from above, body axis perfectly horizontal,
head pointing to the RIGHT edge of the frame,
silhouette left-right symmetric so it reads the same mirrored.
Flat thick-outline cartoon style: solid cel colors,
one bold uniform dark navy (#1C2843) outline around every shape,
no gradients, no texture, only one darker flat tone for the lower half.
Colors: <识别色 hex> body with <更深的同系色> markings, dark navy legs/head.
<该物种的 2~3 个剪影特征，例如两根尾管 / 折叠的弹簧后腿 / 中缝一分为二的硬壳>.
Isolated single creature centered in frame filling about 80 percent of the canvas.
Background is one completely flat solid rose pink color (#C93F6B),
no drop shadow, no glow, no ground, no vignette, no border, no text, no watermark.
Mobile game asset, high contrast, readable at <逻辑尺寸> pixels.
```

硬性约束，缺一条就会返工：

| 约束 | 为什么 |
|---|---|
| 底色写 `#C93F6B` 玫瑰红，**不要写 magenta/#FF00FF** | 模型给不纯，品红会连带把主体边缘染粉；实测这个红抠得最干净 |
| 俯视 + 头朝右 + 上下镜像对称 | 这套模板出的是会旋转的批次（`SPRITE_ROTATES = true`），不对称的图一转就"歪"；正面朝上的批次可省掉头朝右 |
| 单一主体、居中、占 80% 画幅 | 抠图后按包围盒裁边再等比缩放，偏心等于白做 |
| 明确禁止阴影/发光/渐变/地面/文字 | 阴影会被当成主体的一部分保留下来 |
| 识别色 hex 直接写进 prompt | 玩家靠颜色分辨怪类型，颜色错了不能要；病原体本身没有颜色，这块完全由游戏说了算 |
| 尺寸下限写进 prompt（readable at N px） | 否则细节多到 20px 上糊成一团 |

### 10.2 跑一次流水线

1. 出图存到 `vibe_images/`，文件名 `<前缀>_<描述>_v<n>.png`（前缀必须是脚本 `$jobs` 里登记的 `player_idle` / `mob_basic` / `mob_fast` / `mob_tank` / `mob_ranged` / `mob_chest` / `boss1`，同前缀取最新一张）
2. `powershell -NoProfile -ExecutionPolicy Bypass -File tools/sprite_keying.ps1 -Sheet -Zoom 3`
3. 看 `vibe_images/contact_sheet.png`：底色是真实地面色 `#1a1a2e`，缩放倍率统一，能直接判断相对大小和边缘残留

### 10.3 已知坑

- 每张生成图右下角有 "Qoder AI 生成" 水印带，脚本会先把这块涂成底色再抠
- 抠底用**全局色距 + 半透明过渡 + 反预乘**，再按连通块保留：**只丢面积小于最大块 5% 的部分**。蜣螂的粪球是独立连通块，早期"只留最大块"会把它吃掉
- **不要**改用边界洪水填充抠底：厚描边卡通的内部平涂色差比背景渐变还小，填充会直接漏进身体
- `tools/*.ps1` 必须保持纯 ASCII：Windows PowerShell 5.1 按 ANSI 读无 BOM 的 UTF-8，中文注释会报奇怪的引号错误
